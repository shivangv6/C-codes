#include <iostream>
#include<vector>

using namespace std;

int main(){
   int input;
   cin>>input;

   vector<int> vec{0,1};

if(input==1){cout<<vec[0]<<" ";}
else{   
   for(int i=0;i<input-2;i++){
      int rand=vec[i]+vec[i+1];
      vec.push_back(rand);
 }


   for(int element: vec){cout<<element<<" ";}
 }
}
